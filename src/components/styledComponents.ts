import { styled } from '@mui/system';

export const WrapperNavbar = styled('div')({
    display: 'flex',
    alignItems: 'center',
    gap: '10px',
    width: '100%',
    height: '50px',
    background: '#040441bf',
    padding: '0 40px',
    fontSize: '1.2rem',
});
